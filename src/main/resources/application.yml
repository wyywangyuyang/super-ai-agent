spring:
  application:
    name: super-ai-agent
  profiles:
    active: local
  # PgVectorStore 配置
  ai:
    vectorstore:
      pgvector:
        index-type: HNSW
        dimensions: 1536
        distance-type: COSINE_DISTANCE
        max-document-batch-size: 10000
    # MCP 客户端配置
    mcp:
      client:
        # stdio模式
        stdio:
          servers-configuration: classpath:mcp-servers.json
        # sse模式，不要和stdio模式同时启用，需要手动启动mcp服务端，并注释stdio，打开sse
#        sse:
#          connections:
#            server1:
#              url: http://localhost:8127

server:
  port: 8123
  servlet:
    context-path: /api

# 接口文档
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.yy.superaiagent.controller
# knife4j 高级配置
knife4j:
  enable: true
  setting:
    language: zh_cn
